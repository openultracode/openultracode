{
  "name": "openultracode",
  "version": "0.1.0",
  "description": "Local CLI for parallel coding agents with adaptive model routing.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "openultracode": "./dist/bin/ouc.js",
    "ouc": "./dist/bin/ouc.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^6.0.3",
    "vitest": "^4.0.15"
  },
  "engines": {
    "node": ">=20.18"
  }
}
