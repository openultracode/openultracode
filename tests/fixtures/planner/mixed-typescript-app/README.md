# Mixed TypeScript App

Small planner fixture for source, test, and docs scope selection.

## Current Behavior

- Source lives in `src/`.
- Tests live in `tests/`.
- Contributor docs live in `README.md` and `docs/`.
